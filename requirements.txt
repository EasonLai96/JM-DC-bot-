# JM-DC-bot- 依賴套件
#
# 安裝：
#     pip install -r requirements.txt
#
# ⚠️ 版本說明
# -----------
# 下面的版本是依「本專案實際跑得起來的環境」標註的（見 `pip freeze` 結果），
# 並保留遠端 repo 原本的 >= 下限，因此安裝時能得到相容且較新的版本。
# 若某個套件在新版壞掉，請把該行的下限往上調整（而不是移除整套）。

# ── 核心：Discord 機器人框架 ──────────────────────────────────────
discord.py>=2.3.0

# ── 非同步 HTTP（nhentai API、Pixeldrain 上傳、翻譯端點、圖片下載）──
aiohttp>=3.8.0

# ── 禁漫（JM）下載核心 ───────────────────────────────────────────
# 這個套件本身會拉進 curl-cffi / pillow / pycryptodome / pyyaml / commonx，
# 所以下面那幾個雖然沒有直接 import，仍是**必需的傳遞依賴**，不能拿掉。
jmcomic>=2.7.0

# ── 圖片處理與 PDF 產生（漫畫 → PDF）──────────────────────────────
Pillow>=10.0.0
img2pdf>=0.5.0

# ── 讀取 token.env / .env ────────────────────────────────────────
python-dotenv>=1.0.0

# ── 以下是 jmcomic 的傳遞依賴，明確列出以確保環境一致 ──────────────
# （jmcomic 的 METADATA 標示 Requires-Dist，缺一不可）
curl-cffi>=0.6.0
pycryptodome>=3.19.0
PyYAML>=6.0
commonx>=0.6.38

# ── 選用：RAM 監控 ───────────────────────────────────────────────
# bot_monitor.py 會在 import 失敗時嘗試自動安裝，裝不起來也只會退化為
# 「無法顯示 RAM」而不影響機器人啟動。1GB RAM 的環境建議裝上以便觀察。
psutil>=5.9.0
